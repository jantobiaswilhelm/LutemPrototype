# Lutem Production Environment Variables
# Copy this to your deployment platform (Railway, etc.)

# === Required ===

# PostgreSQL connection (Railway provides this automatically)
SPRING_DATASOURCE_URL=jdbc:postgresql://host:port/database
PGUSER=postgres
PGPASSWORD=

# JWT secret (generate a random 64+ char string)
JWT_SECRET=

# Frontend URL for auth redirects
FRONTEND_URL=https://your-frontend.netlify.app

# === Optional ===

# Steam API key (https://steamcommunity.com/dev/apikey)
STEAM_API_KEY=

# Steam OpenID return URLs
STEAM_RETURN_URL=https://your-backend.up.railway.app/auth/steam/callback
STEAM_REALM=https://your-backend.up.railway.app

# Anthropic API key for AI features
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# CORS additional origins (comma-separated)
CORS_EXTRA_ORIGINS=

# Firebase credentials path (for Google auth)
FIREBASE_CREDENTIALS_PATH=firebase-service-account.json

# Connection pool size
HIKARI_MAX_POOL=10

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_RPM=60

# Development mode (set to false in production)
LUTEM_DEV_MODE=false
