{
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.4.0"
  },
  "lint-staged": {
    "frontend-react/src/**/*.{ts,tsx}": [
      "eslint --fix --no-warn-ignored"
    ]
  }
}
